# Legacy Code

This folder contains historical code that supports the story of how the project evolved.

- `pid_experiments/`: old controller variants moved out of the cleaned main ROS package
- `real_world_integration/`: recovered launch files related to real-world ROS setup experiments

Use this archive for research history, not as the default starting point.
