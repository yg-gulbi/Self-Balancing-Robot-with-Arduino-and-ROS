# PID Experiments

This folder stores older balancing controller variants that were originally kept under the ROS control package.

These files are useful for understanding the evolution from direct command handling to the `/before_vel` design, but they are not the default controller entry point for this cleaned repository.
