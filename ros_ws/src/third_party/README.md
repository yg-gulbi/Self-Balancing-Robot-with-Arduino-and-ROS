# Third Party Packages

Place external ROS dependencies here when reproducing old simulation workflows.

Examples from the recovered project history:

- `rtabmap_ros`
- `realsense_ros_gazebo`

This directory is ignored by default so the repository stays lightweight.
